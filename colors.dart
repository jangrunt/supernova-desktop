import 'package:flutter/material.dart';

class AppColors {
  static const brandLightBlue = Color(0xff66ccff); 
  
  static const brandCoral = Color(0xffff5349); 
  
  static const brandDarkBlue = Color(0xff003366); 
  
  /// Reflects the text.primary variable from the theme object
  static const textPrimary = Color(0xff404040); 
  
  /// Reflects the text.secondary variable from the theme object
  static const textSecondary = Color(0xff576471); 
  
  /// Reflects the text.disabled variable from the theme object
  static const textDisabled = Color(0xffc0c0c0); 
  
  /// Reflects the text.accent variable from the theme object
  static const textAccent = Color(0xff003366); 
  
  /// Reflects the primary,success,warning, error.contrast variable from the theme object. Color that keeps a contrast ratio above AA when XX.main is used as a bg. color
  static const textContrast = Color(0xffffffff); 
  
  /// Reflects the primary.main variable from the theme object
  static const primaryMain = Color(0xff0090e6); 
  
  /// Used for hover states. Reflects the primary.dark variable from the theme object
  static const primaryDark = Color(0xff00588c); 
  
  /// Reflects the primary.light variable from the theme object
  static const primaryLight = Color(0xff91c4f5); 
  
  /// The token represents 30% of the Primary/Main token.
  static const primaryLighter = Color(0xffbeddfa); 
  
  /// The token represents 12% of the Primary/Main token.
  static const primarySmooth = Color(0xffe4f1fc); 
  
  /// The token represents 8% of the Primary/Main token.
  static const primarySmoother = Color(0xffeef6fe); 
  
  /// Reflects the secondary.main variable from the theme object
  static const secondaryMain = Color(0xffb0bec5); 
  
  /// Used for hover states. Reflects the secondary.dark variable from the theme object
  static const secondaryDark = Color(0xff90a4ae); 
  
  /// Reflects the secondary.light variable from the theme object
  static const secondaryLight = Color(0xffd7dee1); 
  
  /// The token represents 30% of the Secondary/Main token.
  static const secondaryLighter = Color(0xffe8ecee); 
  
  /// The token represents 12% of the Secondary/Main token.
  static const secondarySmooth = Color(0xfff5f7f8); 
  
  /// The token represents 8% of the Secondary/Main token.
  static const secondarySmoother = Color(0xfff9fafb); 
  
  /// Reflects the warning.main variable from the theme object
  static const warningMain = Color(0xffe06d06); 
  
  /// Used for hover states. Reflects the warning.dark variable from the theme object
  static const warningDark = Color(0xff9c4c04); 
  
  /// Reflects the warning.light variable from the theme object
  static const warningLight = Color(0xfff5b580); 
  
  /// The token represents 30% of the Warning/Main token.
  static const warningLighter = Color(0xfffad3b4); 
  
  /// The token represents 12% of the Warning/Main token.
  static const warningSmooth = Color(0xfffcede0); 
  
  /// The token represents 8% of the Warning/Main token.
  static const warningSmoother = Color(0xfffef4eb); 
  
  /// Reflects the error.main variable from the theme object
  static const errorMain = Color(0xfff22b29); 
  
  /// Used for hover states. Reflects the error.dark variable from the theme object
  static const errorDark = Color(0xffa91e1c); 
  
  /// Reflects the error.light variable from the theme object
  static const errorLight = Color(0xffe89696); 
  
  /// The token represents 30% of the Error/Main token.
  static const errorLighter = Color(0xfff2c1c1); 
  
  /// The token represents 12% of the Error/Main token.
  static const errorSmooth = Color(0xfff9e6e6); 
  
  /// The token represents 8% of the Error/Main token.
  static const errorSmoother = Color(0xfffcefef); 
  
  /// Reflects the info.main variable from the theme object
  static const infoMain = Color(0xff408ab4); 
  
  /// Used for hover states. Reflects the info.dark variable from the theme object
  static const infoDark = Color(0xff2c607d); 
  
  /// Reflects the info.light variable from the theme object
  static const infoLight = Color(0xff80c3e7); 
  
  /// The token represents 30% of the Info/Main token.
  static const infoLighter = Color(0xffb4dcf2); 
  
  /// The token represents 12% of the Info/Main token.
  static const infoSmooth = Color(0xffe0f0f9); 
  
  /// The token represents 8% of the Info/Main token.
  static const infoSmoother = Color(0xffebf6fc); 
  
  static const backgroundMain = Color(0xffffffff); 
  
  static const backgroundUserInput = Color(0xfff2f5f7); 
  
  static const backgroundModalUserInput = Color(0x33ffffff); 
  
  static const backgroundModalMain = Color(0x99ffffff); 
  
  static const navigationPhoneMain = Color(0xff0288d1); 
  
  static const navigationPhoneLight = Color(0xff9acfed); 
  
  static const navigationPhoneBackground = Color(0xfff6fafc); 
  
  static const navigationActivitiesMain = Color(0xff5e35b1); 
  
  static const navigationActivitiesLight = Color(0xffbfaee0); 
  
  static const navigationActivitiesBackground = Color(0xfffaf8fc); 
  
  static const navigationQueuesMain = Color(0xff9c4c04); 
  
  static const navigationQueuesLight = Color(0xffd7b79b); 
  
  static const navigationQueuesBackground = Color(0xfffaf6f3); 
  
  static const navigationDevicesMain = Color(0xff1b5e20); 
  
  static const navigationDevicesLight = Color(0xffa4bfa6); 
  
  static const navigationDevicesBackground = Color(0xfff8fbf8); 
  
  /// Reflects the success.main variable from the theme object
  static const successMain = Color(0xff279500); 
  
  /// Used for hover states. Reflects the success.dark variable from the theme object
  static const successDark = Color(0xff195e01); 
  
  /// Reflects the success.light variable from the theme object
  static const successLight = Color(0xff92c97f); 
  
  /// The token represents 30% of the Success/Main token.
  static const successLighter = Color(0xffbfe0b3); 
  
  /// The token represents 12% of the Success/Main token.
  static const successSmooth = Color(0xffe5f2e0); 
  
  /// The token represents 8% of the Success/Main token.
  static const successSmoother = Color(0xffeef7eb); 
  
  static const borderModal = Color(0xffffffff); 
  
  static const otherDisabled = Color(0xffd1d6d9); 
  
  AppColors._();
}
