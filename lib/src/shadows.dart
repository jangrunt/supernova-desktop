import 'package:flutter/material.dart';

class AppShadows {
  /// Used for Header and Footer component
  static const appDropshadow = BoxShadow(
    color: Color(0x1f000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  /// Used for User inpurt components
  static const userInputShadow = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const phoneDropshadow = BoxShadow(
    color: Color(0x05039be5),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const phoneDropshadow2 = BoxShadow(
    color: Color(0x0a039be5),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const phoneDropshadow3 = BoxShadow(
    color: Color(0x14039be5),
    offset: Offset(0, 2),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const activitiesDropshadow = BoxShadow(
    color: Color(0x055e35b1),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const activitiesDropshadow2 = BoxShadow(
    color: Color(0x0a5e35b1),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const activitiesDropshadow3 = BoxShadow(
    color: Color(0x145e35b1),
    offset: Offset(0, 2),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const queuesDropshadow = BoxShadow(
    color: Color(0x059c4c04),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const queuesDropshadow2 = BoxShadow(
    color: Color(0x0a9c4c04),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const queuesDropshadow3 = BoxShadow(
    color: Color(0x149c4c04),
    offset: Offset(0, 2),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const devicesDropshadow = BoxShadow(
    color: Color(0x051b5e20),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const devicesDropshadow2 = BoxShadow(
    color: Color(0x0a1b5e20),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const devicesDropshadow3 = BoxShadow(
    color: Color(0x141b5e20),
    offset: Offset(0, 2),
    blurRadius: 1,
    spreadRadius: 0,
  );

  AppShadows._();
}
